# Regression Analysis With Python

A **Python-based GUI application** for analyzing custom csv file data using **Multiple Linear Regression**. This app allows users to either use **random pre-defined data** or **input custom data** to evaluate regression patterns and predict the outcome. It also provides visualizations to assess model performance and residuals.

---

## 📝 Features

- **Random Test Mode**  
  Quickly analyze a small pre-defined dataset to see how the regression model performs.

- **Custom Input Mode**  
  Add your own traffic data including:
  - Cars
  - Bikes
  - Trucks
  - Hour of the day
  - Weather condition
  - Traffic Flow  

  Then train a multiple regression model on the data.

- **Metrics Displayed**
  - R² Score – How well the model explains the variance.
  - MAE (Mean Absolute Error) – Average prediction error.
  - MSE (Mean Squared Error) – Squared error of predictions.
  - RMSE (Root Mean Squared Error) – Standard deviation of prediction errors.

- **Visualizations**
  - Actual vs Predicted traffic
  - Residual Error plot
  - Feature importance
  - Correlation scatter plot

- **Interactive Table**  
  Add multiple rows of custom traffic data before training the model.

- **User-Friendly UI**  
  Colorful buttons, playful fonts, and easy-to-use layout.

---

## ⚡ Language and Tools Used

- Python 3.14  
- Tkinter – GUI interface  
- NumPy & Pandas – Data handling  
- Matplotlib – Plotting and visualizations  
- scikit-learn – Linear Regression & metrics
- Code editor: VS Code

---

## 🚀 How to Use

1. **Clone or download** the repository.
2. Ensure required packages are installed:
3. Use a code editor like vsc / atom then use it to run the project file

   ```bash
   pip install numpy pandas matplotlib scikit-learn
